import { darkColors } from "./darkColors";
import { lightColors } from "./lightColors";

export const themes = {
    dark: darkColors,
    light: lightColors,
};

export const radius = {
    sm: 10,
    md: 16,
    lg: 24,
};

export const spacing = {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
};